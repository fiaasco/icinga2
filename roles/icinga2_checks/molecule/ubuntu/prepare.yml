---
- name: Prepare
  hosts: all
  become: true
  pre_tasks:
    - name: install prerequisites
      package:
        name:
          - mariadb-server
          - ufw
        state: present

    - name: enable services
      service:
        name: "{{ item }}"
        enabled: true
        state: started
      with_items:
        - ufw
        - mysql

  roles:
    - role: icinga2-master
